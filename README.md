# Разработка архитектуры оркестратора сборки на примере BuildStream

## Предметная область

**Разработчик (Инженер сборки)** — физическое лицо, использующее оркестратор для сборки программных компонентов. Разработчик определяет структуру проекта (создаёт файлы .vtk), запускает процесс сборки локально или на серверах CI, управляет зависимостями и анализирует результаты. Может выступать в роли потребителя артефактов (получает готовые бинарные пакеты из кеша) или поставщика (публикует новые версии артефактов в удалённое хранилище). Имеет потребность в детерминированной и воспроизводимой среде для исключения проблем «на моей машине работает».

**Проект (Project)** — корневой репозиторий исходного кода, содержащий декларативное описание компонентов сборки (файлы .vtk и конфигурацию project.conf). Определяет глобальные зависимости и параметры окружения для всей иерархии сборки.

**Элемент (Element)** — атомарная единица сборки, описанная в файле .vtk. Соответствует отдельному программному компоненту, библиотеке или исполняемому файлу. Характеризуется набором исходного кода, зависимостями от других элементов и правилами интеграции артефактов в окружение сборки (песочницу).

**Источник (Source)** — указатель на внешний ресурс: Git-репозиторий, архив с кодом, HTTP-ссылку или локальный путь. Позволяет системе детерминированно получить состояние исходного кода по фиксированной контрольной сумме или метке времени.

**Артефакт (Artifact)** — результат успешной сборки элемента. Представляет собой неизменяемый срез файловой системы (директорию сборки), адресуемый по хешу содержимого (Content Addressable Storage - CAS).

**Песочница (Sandbox)** — изолированная среда выполнения (контейнер на базе bubblewrap или иного механизма), в которой запускаются команды сборки. Гарантирует отсутствие влияния хостовой системы и сетевых запросов (если они явно не разрешены) на воспроизводимость результата.

**Кеш-сервер (Artifact Cache)** — удаленное или локальное хранилище, реализующее протокол CAS. Используется для сохранения артефактов, чтобы избежать повторной сборки элемента при неизменных входных данных и зависимостях.

**Платформа - среда исполнения (Platform / Arch)** — совокупность архитектурных параметров (архитектура CPU, libc, целевая ОС), для которой производится сборка. Система кеширует артефакты отдельно для каждой уникальной комбинации параметров платформы.

**Плагин сборки (Build Plugin)** — модуль, определяющий способ исполнения стадий configure, build и install для конкретной экосистемы (например, autotools, cmake, meson, python). Отвечает за генерацию команд, выполняемых в песочнице.

### Диаграмма классов предметной области

![Диаграмма классов предметной области](class.drawio.png)

## Прецеденты

### Диаграмма прецедентов

![Диаграмма прецедентов](precedent.drawio.png)

### Описание основных функций

#### Актор: Разработчик (инженер сборки)

1. **Инициализировать проект**. Разработчик может создать новый проект сборки в локальной директории. Система генерирует файлы конфигурации (project.conf) и базовую структуру для описания элементов.

2. **Собрать компонент / проект**. Разработчик указывает целевой элемент (или проект целиком). Система вычисляет граф зависимостей, при необходимости загружает недостающие артефакты из кеша, запускает сборку в изолированной песочнице и сохраняет результат в локальное CAS-хранилище.

3. **Загрузить артефакты из кеша (pull)**. Разработчик явно запрашивает загрузку артефактов для заданных элементов из настроенных удалённых кеш-серверов. Система проверяет наличие по хешам и при успехе заполняет локальное CAS-хранилище.

4. **Отправить артефакты в кеш (push)**. После успешной сборки разработчик отправляет полученные артефакты в удалённый кеш-сервер для совместного использования командой. Система передаёт только те артефакты, которые отсутствуют на сервере.

5. **Открыть оболочку в песочнице**. Разработчик запускает интерактивную командную оболочку внутри изолированной среды сборки определённого элемента. Используется для отладки, ручного тестирования или воспроизведения окружения.

6. **Настроить удалённые кеш-серверы**. Разработчик редактирует конфигурацию проекта или глобальные настройки, указывая URL, приоритеты и параметры аутентификации для внешних хранилищ артефактов.

7. **Просмотреть состояние сборки / зависимости**. Разработчик запрашивает информацию о текущем состоянии элементов: какие артефакты кешированы локально, какие требуют сборки, от каких элементов зависит данный компонент.

8. **Очистить локальный кеш артефактов**. Разработчик удаляет неиспользуемые или устаревшие артефакты из локального CAS-хранилища для освобождения дискового пространства.

9. **Проверить целостность проекта / кеша**. Разработчик запускает проверку соответствия контрольных сумм источников и артефактов. Система валидирует целостность данных и сообщает о возможных повреждениях.

10. **Просмотреть логи сборки**. Разработчик изучает журналы выполнения команд сборки для диагностики ошибок или анализа производительности.

---

## 1. Функциональные требования

1.1. **Декларативное описание элементов.**  
Система обеспечивает описание компонентов сборки (элементов) в декларативных текстовых файлах (например, YAML или специализированный DSL), без необходимости писать императивные скрипты верхнего уровня.

1.2. **Управление зависимостями.**  
Система автоматически вычисляет и разрешает граф зависимостей между элементами на основе объявленных в файлах описания связей.

1.3. **Детерминированное получение источников.**  
Для каждого элемента система загружает исходный код из внешних источников (Git, архив, HTTP) с обязательной проверкой целостности по криптографическому хешу (SHA-256). Изменение состояния удалённого репозитория не влияет на сборку без явного обновления контрольной суммы.

1.4. **Кеширование артефактов на основе содержимого (CAS).**  
Система вычисляет уникальный идентификатор (хеш) для каждого собранного артефакта, основанный на входных данных, зависимостях, параметрах платформы и версии плагина сборки. При повторном запросе сборки с идентичным набором входных параметров артефакт извлекается из кеша без выполнения команд сборки.

1.5. **Загрузка артефактов из удалённого кеша (pull).**  
Система позволяет настраивать подключение к удалённым кеш-серверам и загружать отсутствующие артефакты из удалённого хранилища.

1.6. **Выгрузка артефактов в удалённый кеш (push).**  
Система позволяет отправлять локально собранные артефакты в удалённое хранилище для совместного использования.

1.7. **Изоляция среды сборки.**  
Все команды сборки выполняются внутри строгой файловой песочницы. Доступ к хостовой файловой системе разрешается только на чтение для монтируемых зависимостей; запись разрешена только в выделенную директорию вывода.

1.8. **Поддержка плагинов систем сборки.**  
Архитектура системы позволяет подключать плагины, реализующие логику сборки для различных экосистем (Make, CMake, Meson, Autotools и др.) без модификации ядра оркестратора.

1.9. **Локальное кеширование источников.**  
Система ведёт локальный репозиторий для зеркалирования загруженных исходных кодов (Git-репозиториев, архивов), обеспечивая возможность работы в офлайн-режиме.

1.10. **Интерактивная отладка в песочнице.**  
Разработчик имеет возможность открыть интерактивную командную оболочку внутри изолированной среды сборки для отладки или ручного тестирования.

1.11. **Проверка целостности кеша и проекта.**  
Система предоставляет команду для проверки соответствия контрольных сумм всех артефактов и источников, выявляя повреждённые или изменённые данные.

1.12. **Просмотр состояния сборки.**  
Разработчик может получить информацию о текущем состоянии элементов: какие артефакты кешированы, какие ожидают сборки, а также визуализировать граф зависимостей.

1.13. **Очистка локального кеша.**  
Система позволяет удалять неиспользуемые или устаревшие артефакты из локального CAS-хранилища для освобождения дискового пространства.

1.14. **Логирование процесса сборки.**  
Система сохраняет подробные журналы выполнения каждой стадии сборки (стандартный вывод, ошибки, время выполнения) с возможностью последующего просмотра.

---

## 2. Нефункциональные требования

2.1. **Интуитивный синтаксис описания элементов.**  
Формат файлов `.vtk` должен быть читаемым и позволять типовую сборку описать не более чем 10 строками декларативного кода.

2.2. **Информативные сообщения об ошибках.**  
При сбое сборки система выводит локализованное описание причины и ссылку на соответствующий лог-файл.

2.3. **Прогресс выполнения.**  
При длительных операциях (сборка, push/pull) система предоставляет информацию о ходе выполнения с указанием текущего элемента и оставшегося времени.

2.4. **Воспроизводимость сборок.**  
При одинаковых входных данных (хеш источников, зависимости, конфигурация платформы) результат сборки должен быть побайтово идентичным на любых машинах и в любое время.

2.5. **Устойчивость к прерываниям.**  
При аварийном завершении процесса сборки или обрыве сетевого соединения локальное состояние проекта не переходит в неконсистентное. Допускается возобновление с последнего неудачного шага.

2.6. **Атомарность операций с кешем.**  
Загрузка и выгрузка артефактов выполняются транзакционно: частично загруженный артефакт не считается валидным и подлежит повторной загрузке.

2.7. **Скорость загрузки из кеша.**  
Получение артефакта из локального CAS-хранилища (восстановление из кеша) выполняется не более чем за 2 секунды для артефакта размером до 1 ГБ.

2.8. **Эффективность удалённого кеширования.**  
При доступности удалённого кеш-сервера с пропускной способностью не менее 100 Мбит/с время загрузки артефакта объёмом 500 МБ не превышает 60 секунд.

2.9. **Параллельное выполнение.**  
Планировщик очереди сборки обеспечивает безопасное параллельное выполнение несвязанных элементов с коэффициентом использования CPU не менее 90% на многоядерных системах.

2.10. **Минимальное влияние хостовой системы.**  
Время запуска песочницы и подготовки окружения не превышает 1 секунду на элемент.

2.11. **Логирование действий.**  
Система ведёт журнал всех операций (инициализация, сборка, push/pull, очистка) с указанием временных меток, пользователя и результатов.

2.12. **Версионирование плагинов.**  
Плагины сборки поддерживают независимое версионирование; изменение версии плагина приводит к инвалидации соответствующих кешированных артефактов.

2.13. **Расширяемость.**  
Добавление нового плагина сборки или нового типа источника не требует модификации исходного кода ядра системы.

---

## 3. Ограничения проектирования

3.1. **Отсутствие глобального состояния.**  
Оркестратор не хранит метаданные о сборках в единой системной базе данных, доступной разным пользователям. Вся информация о состоянии хранится в рабочей директории проекта и CAS-хранилище.

3.2. **Требования к файловой системе.**  
Директория локального кеша должна располагаться на файловой системе, поддерживающей создание жёстких ссылок или reflink-копий (например, Btrfs, XFS, ZFS, APFS) для эффективного копирования артефактов.

3.3. **Реализация изоляции.**  
Механизм изоляции должен основываться на возможностях ядра Linux (namespaces, seccomp) и утилите `bubblewrap` как обязательной runtime-зависимости для обеспечения кросс-дистрибутивной совместимости.

3.4. **Совместимость формата файлов.**  
Формат файлов описания элементов (`Element.vtk`) должен сохранять обратную совместимость в рамках одной мажорной версии системы.

3.5. **Лицензионная чистота.**  
Система и её зависимости должны распространяться под лицензиями, допускающими коммерческое использование без ограничений (например, Apache 2.0, MIT, LGPL).

3.6. **Интеграция с CI/CD.**  
Система предоставляет интерфейс командной строки, позволяющий встраивать её в конвейеры непрерывной интеграции (GitLab CI, GitHub Actions, Jenkins).

3.7. **Документирование.**  
Для каждого поддерживаемого плагина сборки должно быть представлено описание и минимальный рабочий пример.

3.8. **Минимальные требования.**  
Система должна работать на оборудовании с объёмом оперативной памяти не менее 4 ГБ и свободным дисковым пространством не менее 10 ГБ для кеша.

3.9. **Поддержка архитектур.**  
Система обеспечивает кросс-компиляцию для целевых архитектур x86_64, ARM64 (aarch64) и RISC-V при наличии соответствующих инструментальных цепочек в проекте.

---

## 2.4 Архитектурные мотивы

В таблице ниже представлены ключевые архитектурные мотивы, оказывающие влияние на проектные решения при разработке оркестратора сборки. Мотивы сгруппированы по характеру воздействия на архитектуру и снабжены ссылками на соответствующие функциональные и нефункциональные требования.

| Мотив | Номера требований | Обоснование |
|-------|-------------------|-------------|
| **Детерминизм и воспроизводимость сборок** | 1.3, 1.4, 2.4 | Требование побайтовой идентичности артефактов при одинаковых входных данных накладывает строгие ограничения на управление состоянием: все зависимости должны быть фиксированы по хешу, среда сборки полностью изолирована от хоста, а процесс сборки не должен зависеть от времени или порядка выполнения. Это влияет на выбор модели данных (CAS) и механизма изоляции. |
| **Изоляция среды выполнения** | 1.7, 3.3 | Выполнение команд сборки в строгой песочнице без доступа к хосту и сети требует внедрения механизма контейнеризации на уровне ядра ОС. Это определяет использование `bubblewrap` или аналогичных инструментов и влияет на структуру рантайма оркестратора. |
| **Кеширование на основе содержимого (CAS)** | 1.4, 2.7, 2.8 | Хранение артефактов по хешу содержимого, а не по имени или пути, принципиально меняет способ адресации данных. Это требует реализации CAS-хранилища, расчёта хешей для всех входных параметров и механизма композиции артефактов через жёсткие ссылки/reflink. |
| **Удалённое кеширование и совместное использование артефактов** | 1.5, 1.6, 2.5, 2.6 | Возможность загрузки (pull) и выгрузки (push) артефактов на удалённые серверы требует определения сетевого протокола для CAS (например, на базе gRPC с поддержкой потоковой передачи), политик аутентификации и стратегий обработки частичных сбоев. |
| **Модульность и расширяемость через плагины** | 1.8, 2.12, 2.13 | Поддержка различных систем сборки (Make, CMake, Meson и др.) без изменения ядра требует чёткого интерфейса плагинов. Это влияет на декомпозицию системы, механизм динамической загрузки модулей и управление версиями плагинов для инвалидации кеша. |
| **Параллельное выполнение и эффективное использование ресурсов** | 2.9, 2.10 | Сборка независимых элементов должна выполняться параллельно для сокращения общего времени. Это требует реализации планировщика задач, который учитывает граф зависимостей, доступные вычислительные ресурсы и блокировки на уровне кеша. |
| **Эффективная работа с файловой системой** | 3.2 | Операции копирования артефактов из кеша в песочницу должны быть мгновенными и не расходовать лишнее дисковое пространство. Это обуславливает использование возможностей современных ФС (reflink на Btrfs/XFS/ZFS или жёсткие ссылки) и влияет на логику компоновки окружения сборки. |
| **Кроссплатформенная и кросс-архитектурная сборка** | 3.9 | Система должна позволять собирать проект под целевую архитектуру, отличную от хостовой (например, ARM64 на x86_64). Это требует отдельного хранения артефактов для каждой платформы в CAS, а также поддержки эмуляции или кросс-компиляционных тулчейнов в песочнице. |
| **Интеграция с CI/CD и автоматизация** | 3.6 | Оркестратор должен легко встраиваться в конвейеры непрерывной интеграции (GitLab CI, GitHub Actions). Это влияет на интерфейс командной строки (возвращаемые коды, машиночитаемый вывод) и возможность неинтерактивного запуска с передачей секретов для доступа к кеш-серверам. |
| **Надёжность и устойчивость к прерываниям** | 2.5, 2.6 | Сбои сети или отмена процесса не должны приводить к повреждению локального кеша или неконсистентному состоянию проекта. Это требует транзакционных операций записи (атомарное переименование, контрольные суммы) и возможности возобновления сборки с прерванного шага. |
| **Логирование и диагностика** | 1.14, 2.11 | Для отладки сбоев и анализа производительности необходимо сохранять структурированные логи сборки каждого элемента. Это влияет на способ хранения логов (привязка к элементу/артефакту) и механизмы ротации/очистки. |
